// En App.js - Solo agregas esto:
{
  id: 'properties',
  name: 'Propiedades', 
  sections: [
    { id: 'listings', name: 'Listados' },
    { id: 'evaluations', name: 'Evaluaciones' },
    { id: 'analytics', name: 'Analytics' },
    { id: 'reports', name: 'Reportes' }
  ]
}

// En ModuleRenderer.js - Solo agregas esto:
if (activeModule === 'properties') {
  if (currentSection?.id === 'listings') return <PropertiesListings />;
  if (currentSection?.id === 'evaluations') return <PropertiesEvaluations />;
  // etc...
}